import {NextResponse} from 'next/server';import {currentPartner} from '@/lib/partner-auth';import {db} from '@/lib/db';import {chatPreview} from '@/lib/chat';
export async function GET(){const auth=await currentPartner();if(!auth)return NextResponse.json({ok:false},{status:401});const orders=await db.order.findMany({where:{partnerId:auth.partner.id,thread:{isNot:null}},orderBy:{updatedAt:'desc'},take:50,select:{publicId:true,status:true,customer:{select:{name:true}},thread:{select:{id:true,messages:{orderBy:[{createdAt:'desc'},{id:'desc'}],take:1,select:{body:true,role:true,createdAt:true}},readCursors:{where:{userId:auth.user.id},take:1,select:{lastReadAt:true}}}}}});return NextResponse.json({ok:true,conversations:orders.map(o=>({orderId:o.publicId,status:o.status,partnerName:o.customer.name,partnerLogo:null,lastMessage:chatPreview(o.thread?.messages[0]?.body),lastMessageAt:o.thread?.messages[0]?.createdAt||null,lastRole:o.thread?.messages[0]?.role||null,readAt:o.thread?.readCursors[0]?.lastReadAt||null}))})}
